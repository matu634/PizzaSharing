@model WebApp.ViewModels.ReceiptRowChangeViewModel

@{
    ViewData["Title"] = "Edit";
}

<h1>Edit</h1>
<h4>Receipt Row Change</h4>
<hr />


<div class="col-md-6">
    <div class="card card-warning">
        
        @* Card Header *@
        <div class="card-header">
            <h3 class="card-title"><i class="fa fa-edit"></i></h3>
        </div>
        <form role="form" asp-action="Edit">

            @* Card Body *@
            <div class="card-body">
                <div asp-validation-summary="ModelOnly" class="text-danger"></div>

                <div class="form-group">
                    <label asp-for="ReceiptRowChange.Id" class="control-label">Id</label>
                    <input asp-for="ReceiptRowChange.Id" class="form-control" disabled/>
                </div>

                @* Model Specific Fields *@
                <div class="form-group">
                    <label asp-for="ReceiptRowChange.ReceiptRowId" class="control-label">Receipt Row Id</label>
                    <select asp-for="ReceiptRowChange.ReceiptRowId" class="form-control" asp-items="Model.Rows"></select>
                    <span asp-validation-for="ReceiptRowChange.ReceiptRowId" class="text-danger"></span>
                </div>
                <div class="form-group">
                    <label asp-for="ReceiptRowChange.ChangeId" class="control-label">Change Name</label>
                    <select asp-for="ReceiptRowChange.ChangeId" class="form-control" asp-items="Model.Changes"></select>
                    <span asp-validation-for="ReceiptRowChange.ChangeId" class="text-danger"></span>
                </div>
            </div>

            @* Card Footer *@
            <div class="card-footer">
                <div class="form-group">
                    <input type="hidden" asp-for="ReceiptRowChange.Id"/>
                    <input type="submit" value="Save" class="btn btn-primary"/>
                </div>
            </div>
            
        </form>
    </div>
</div>

<form >
    <button class="btn btn-default" asp-area="" asp-action="Index">
        <i class="fa fa-list"></i>
        Back to list
    </button>
</form>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
