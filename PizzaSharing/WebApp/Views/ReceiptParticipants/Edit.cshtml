@model WebApp.ViewModels.ReceiptParticipantViewModel

@{
    ViewData["Title"] = "Edit";
}
<h1>Edit</h1>

<h4>Receipt Participant</h4>
<hr />


<div class="col-md-6">
    <div class="card card-warning">
        
        @* Card Header *@
        <div class="card-header">
            <h3 class="card-title"><i class="fa fa-edit"></i></h3>
        </div>
        <form role="form" asp-action="Edit">

            @* Card Body *@
            <div class="card-body">
                <div asp-validation-summary="ModelOnly" class="text-danger"></div>

                <div class="form-group">
                    <label asp-for="ReceiptParticipant.Id" class="control-label">Id</label>
                    <input asp-for="ReceiptParticipant.Id" class="form-control" disabled/>
                </div>

                @* Model Specific Fields *@
                <div class="form-group">
                    <label asp-for="ReceiptParticipant.ReceiptId" class="control-label">Receipt Id</label>
                    <select asp-for="ReceiptParticipant.ReceiptId" class="form-control" asp-items="Model.Receipts"></select>
                    <span asp-validation-for="ReceiptParticipant.ReceiptId" class="text-danger"></span>
                </div>
                <div class="form-group">
                    <label asp-for="ReceiptParticipant.AppUserId" class="control-label">App User Nickname</label>
                    <select asp-for="ReceiptParticipant.AppUserId" class="form-control" asp-items="Model.AppUsers"></select>
                    <span asp-validation-for="ReceiptParticipant.AppUserId" class="text-danger"></span>
                </div>
            </div>

            @* Card Footer *@
            <div class="card-footer">
                <div class="form-group">
                    <input type="hidden" asp-for="ReceiptParticipant.Id"/>
                    <input type="submit" value="Save" class="btn btn-primary"/>
                </div>
            </div>
            
        </form>
    </div>
</div>

<form >
    <button class="btn btn-default" asp-area="" asp-action="Index">
        <i class="fa fa-list"></i>
        Back to list
    </button>
</form>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
