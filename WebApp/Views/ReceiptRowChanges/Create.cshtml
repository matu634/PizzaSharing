@model Domain.ReceiptRowChange

@{
    ViewData["Title"] = "Create";
}

<h1>Create</h1>

<h4>ReceiptRowChange</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="ReceiptRowId" class="control-label"></label>
                <select asp-for="ReceiptRowId" class ="form-control" asp-items="ViewBag.ReceiptRowId"></select>
            </div>
            <div class="form-group">
                <label asp-for="ChangeId" class="control-label"></label>
                <select asp-for="ChangeId" class ="form-control" asp-items="ViewBag.ChangeId"></select>
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
