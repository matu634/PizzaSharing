@model IEnumerable<Domain.Receipt>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.CreatedTime)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ReceiptManager)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.CreatedTime)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ReceiptManager.Id)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.ReceiptId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.ReceiptId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.ReceiptId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
